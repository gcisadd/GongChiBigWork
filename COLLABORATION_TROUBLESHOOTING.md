# WebSocket 协作功能故障排除指南

## 问题描述
协作功能不生效，无法实时同步编辑内容。

## 可能原因及解决方案

### 1. 后端服务未启动
协作功能需要后端 WebSocket 服务支持。

**检查方法：**
- 打开浏览器控制台 (F12)
- 查看是否有 `[协作] 无法连接到协作服务器` 的错误

**解决方法：**
```bash
# 进入后端目录
cd backend

# 激活虚拟环境（如果需要）
# Windows:
.\venv\Scripts\activate
# 或
.\venv\Scripts\activate.ps1

# 启动服务
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 2. WebSocket 连接失败
WebSocket 连接地址配置问题。

**已修复：**
- WebSocket URL 现在会根据当前页面协议自动选择 `ws:` 或 `wss:`
- 支持通过环境变量 `VITE_WS_HOST` 配置服务器地址

### 3. CORS 跨域问题
浏览器安全策略阻止了 WebSocket 连接。

**已修复：**
- 后端 CORS 配置已更新，支持更多本地地址
- 包括 `localhost:5173`、`localhost:3000`、`localhost:8080` 等

### 4. TypeScript 类型错误

**已修复：**
- 修复了 `generateUserColor` 函数的类型问题

## 测试步骤

### 1. 启动后端服务
```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 2. 启动前端开发服务器
```bash
npm run dev
```

### 3. 测试协作功能
1. 打开浏览器访问 `http://localhost:5173`
2. 登录或使用默认用户
3. 导航到「协作编辑」页面
4. 查看页面顶部是否显示「已连接」状态
5. 打开另一个浏览器窗口（或隐身模式）
6. 使用不同用户登录
7. 观察在线用户列表是否显示多个用户
8. 在一个窗口编辑内容，查看是否实时同步到另一个窗口

## 日志查看

协作功能会在浏览器控制台输出详细信息：
- `[协作] 正在连接到: ws://localhost:8000/ws/collaborate/{documentId}`
- `[协作] WebSocket 连接已建立`
- `[协作] WebSocket 错误:` - 连接失败时的错误信息

## 常见错误代码

| 错误码 | 含义 | 解决方案 |
|--------|------|----------|
| 1000 | 正常关闭 | 可能是用户主动关闭，检查是否误操作 |
| 1001 | 端点离开 | 页面关闭或刷新 |
| 1006 | 异常关闭 | 网络问题或服务器断开，检查后端服务 |

## 联系支持
如果问题仍然存在，请提供以下信息：
1. 浏览器控制台完整错误信息
2. 后端服务启动日志
3. 操作系统版本
4. 浏览器类型和版本
