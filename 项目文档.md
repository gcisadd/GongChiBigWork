# 基于Vue3与FastAPI的协作编辑平台设计与实现

## 文档

---

## 第一章 绪论

### 1.1 研究背景及意义

随着互联网技术的飞速发展和企业数字化转型的深入推进，**协作办公**已成为现代企业和团队提升工作效率的关键手段。传统的单机文档编辑模式已无法满足现代团队协作的需求，多人实时协作编辑、文档共享、版本控制等需求日益迫切。特别是在后疫情时代，远程办公和分布式团队协作已成为新常态，对在线协作工具的需求呈现爆发式增长。

本项目 **GongChiBigWork** 正是在这一背景下应运而生，旨在构建一个功能完善、性能优异的**多人协作编辑平台**。该平台融合了先进的Web前端技术、高效的后端服务架构以及实时通信技术，为用户提供流畅的文档编辑体验和便捷的团队协作能力。

本项目的研究意义主要体现在以下几个方面：

**技术层面**，本项目探索了Vue 3组合式API与FastAPI异步框架的深度融合实践，验证了前后端分离架构在实时协作场景下的可行性。同时，通过WebSocket技术实现的多人实时编辑功能，为类似应用提供了技术参考方案。

**应用层面**，本项目实现的协作编辑平台可直接应用于企业日常办公、在线教育、远程团队协作等场景，具有广泛的实际应用价值和商业前景。

**学习层面**，本项目作为一个完整的全栈开发案例，涵盖了用户认证、数据库设计、RESTful API设计、实时通信、前端工程化等核心技术点，对于学习和提升全栈开发能力具有重要的参考价值。

### 1.2 国内外现状

#### 1.2.1 国外发展现状

在国际市场，在线协作编辑领域已形成较为成熟的生态系统。以Google Docs、Microsoft Office 365、Notion为代表的协作工具占据了主要市场份额。这些产品具有以下特点：

**Google Docs** 凭借Google生态系统的优势，实现了与Gmail、Google Drive等产品的深度集成，支持多人实时协作编辑，提供强大的版本历史功能和离线支持。其核心技术采用了 Operational Transformation (OT) 算法来解决冲突问题。

**Microsoft Office 365** 中的Word Online继承了桌面版Word的强大功能，支持实时协作、评论、批注等高级功能，与OneDrive云存储服务紧密结合，为企业用户提供了完整的办公解决方案。

**Notion** 则以"所有工作集于一处"为理念，将笔记、文档、数据库、项目管理等功能融为一体，采用块编辑器设计理念，支持灵活的内容组织和团队协作。

此外，**Quip**、**Confluence**、**Coda** 等产品也在特定细分领域占据重要地位。

#### 1.2.2 国内发展现状

国内市场同样涌现出众多优秀的协作办公产品。**腾讯文档**、**金山文档**、**飞书文档**、**石墨文档**等产品各具特色：

**腾讯文档** 依托微信和QQ的社交关系链，实现了便捷的文档分享和协作，支持小程序直接编辑，在国内市场占有率较高。

**飞书文档** 作为字节跳动旗下产品，以"先进团队先用飞书"为定位，在文档体验、智能助手、团队协作等方面具有创新性，提供了强大的多维表格和知识库功能。

**石墨文档** 是国内较早专注于在线文档协作的产品，以简洁的界面和流畅的协作体验著称，在中小企业用户中有较高口碑。

然而，当前市场产品仍存在一些痛点：部分产品功能过于复杂、学习成本较高；部分产品免费版功能限制过多；部分产品在实时协作稳定性方面仍有提升空间。这些问题为本项目的研发提供了市场机会。

#### 1.2.3 技术发展趋势

从技术发展趋势来看，在线协作编辑平台正朝着以下方向演进：

1. **AI赋能**：大语言模型的引入为文档创作提供了智能辅助，如AI续写、自动摘要、内容润色等功能正成为标配。

2. **跨平台同步**：移动端与PC端的无缝体验成为基本要求，离线编辑与在线同步技术日趋成熟。

3. **结构化数据**：从纯文本编辑向结构化文档（如Notion的块结构）演进，支持更丰富的内容组织和数据关联。

4. **低代码集成**：与低代码平台、工作流系统的深度集成，使文档成为业务流转的载体。

### 1.3 主要研究工作

本项目围绕协作编辑平台的设计与实现，主要开展了以下研究工作：

**（1）系统架构设计**

采用前后端分离的现代化Web架构，前端使用Vue 3 + TypeScript + Vite构建单页应用，后端使用FastAPI + SQLAlchemy构建RESTful API服务。设计了清晰的分层架构，包括表现层、业务层、数据层，确保系统的可维护性和可扩展性。

**（2）用户认证系统**

实现了基于JWT的用户认证机制，包括用户注册、登录、Token刷新等核心功能。密码进行安全处理，设计了完整的会话管理方案，支持多设备登录和 token 失效机制。

**（3）文档管理系统**

实现了文档的完整生命周期管理，包括创建、编辑、删除、查询等操作。设计了文档分页加载和模糊搜索功能，优化了大数据量场景下的用户体验。集成AI文档概括功能，利用大语言模型自动生成文档摘要。

**（4）实时协作编辑**

基于WebSocket技术实现了多人实时协作编辑功能。设计了客户端协作服务类，管理WebSocket连接和消息同步。实现了光标位置同步和在线用户列表展示，提升协作体验。

**（5）权限与社交系统**

设计了三级权限模型（owner/edit/view），支持文档所有者对协作者权限的精细控制。实现了完整的好友社交系统，包括用户搜索、好友请求、接受/拒绝等社交功能。

**（6）评论系统**

实现了文档评论功能，支持嵌套回复。设计了评论的编辑、删除等管理功能，为用户提供交流和反馈的渠道。

### 1.4 论文结构

本文共分为六章，各章内容安排如下：

**第一章 绪论**：介绍研究背景及意义，分析国内外发展现状，阐述本文的主要研究工作和论文结构。

**第二章 相关技术介绍**：详细介绍项目所涉及的前端技术（Vue 3、TypeScript、Vite、Element Plus、Quill等）和后端技术（FastAPI、SQLAlchemy、WebSocket等），分析各技术的特点和优势。

**第三章 系统需求分析**：进行系统需求分析，包括功能需求分析（用户管理、文档管理、协作编辑、好友系统等）和非功能需求分析（性能、安全、可用性等）。

**第四章 系统设计**：设计系统整体架构，规划功能模块划分，设计数据库表结构，绘制系统架构图。

**第五章 系统实现**：介绍系统通用部分的实现方案，详细阐述核心功能（用户认证、文档管理、实时协作、权限控制、好友系统、评论系统）的实现细节。

**第六章 总结与展望**：总结本文的主要工作成果，分析系统存在的不足，提出未来改进方向。

---

## 第二章 相关技术介绍

### 2.1 前端技术

#### 2.1.1 Vue 3

**Vue 3** 是Vue.js框架的第三个主要版本，于2020年9月正式发布。与Vue 2相比，Vue 3带来了革命性的变化：

**组合式API（Composition API）**：Vue 3引入了全新的组合式API，允许开发者使用函数式的方式组织组件逻辑。相比于Vue 2的选项式API（Options API），组合式API具有更好的逻辑复用性、更灵活的代码组织方式，尤其适合大型复杂应用的开发。本项目大量使用了`ref`、`reactive`、`computed`、`watch`等组合式API。

**性能提升**：Vue 3重写了响应式系统，采用Proxy替代Object.defineProperty，显著提升了数据响应速度。同时，Vue 3的编译器也进行了优化，生成的代码更加高效。根据官方数据，Vue 3的整体性能比Vue 2提升了1.5~2倍。

**TypeScript支持**：Vue 3使用TypeScript重写，提供了更好的类型支持。本项目采用TypeScript进行开发，充分利用了类型系统带来的开发体验提升。

**Vite默认支持**：Vue 3项目默认使用Vite作为构建工具，享受极快的冷启动和热更新体验。

#### 2.1.2 TypeScript

**TypeScript** 是JavaScript的超集，它添加了类型系统和对ES6+特性的支持。本项目选择TypeScript的原因包括：

**类型安全**：TypeScript的静态类型检查可以在编译时发现潜在错误，减少运行时bug。接口（Interface）和类型（Type）定义使得代码可读性和可维护性大幅提升。

**IDE支持**：主流IDE（如VS Code）对TypeScript提供了强大的支持，包括智能提示、代码跳转、重构等功能，显著提升开发效率。

**更好的重构**：类型信息使得代码重构更加安全可靠，降低了修改代码的风险。

本项目在`tsconfig.json`中配置了严格的类型检查选项，确保代码质量。

#### 2.1.3 Vite

**Vite** 是由Vue.js作者尤雨溪开发的新一代前端构建工具，其核心理念是"极速的开发体验"。

**基于ESM**：Vite利用浏览器原生ES模块支持，在开发环境下直接提供源码，无需进行打包。生产环境下使用Rollup进行优化打包。

**极速冷启动**：由于无需打包，Vite的冷启动时间几乎为零，相比Webpack的几十秒等待有了质的飞跃。

**热模块替换（HMR）**：Vite利用ESM实现了高效的热更新，修改代码后只需精确更新变化的模块，无需刷新整个页面。

**内置特性**：Vite内置了TypeScript、CSS预处理器、JSX等支持，配置简单直观。

本项目使用Vite 7.3.0版本，配合Vue 3实现了流畅的开发体验。

#### 2.1.4 Element Plus

**Element Plus** 是为Vue 3打造的桌面端UI组件库，由饿了么团队维护和更新。

**组件丰富**：Element Plus提供了从基础组件（按钮、输入框、表格）到高级组件（日期选择器、对话框、级联选择器）的完整组件生态。

**TypeScript友好**：Element Plus使用TypeScript编写，提供了完整的类型定义，与本项目的TypeScript开发完美契合。

**设计规范**：Element Plus遵循统一的设计规范，组件外观和交互一致性良好，可以快速构建专业的管理后台。

**持续更新**：Element Plus保持活跃的开发和维护，持续跟进Vue 3的新特性和社区需求。

本项目使用Element Plus 2.13.0版本，构建了包括侧边栏、表格、表单、对话框在内的完整UI界面。

#### 2.1.5 Quill富文本编辑器

**Quill** 是一个功能强大的现代富文本编辑器，提供了所见即所得（WYSIWYG）的编辑体验。

**API驱动**：Quill提供了丰富的API，支持自定义工具栏、监听内容变化、获取/设置HTML或Delta格式内容。

**Delta格式**：Quill使用独特的Delta格式描述文档内容变化，不仅包含最终内容，还保留了操作历史，便于实现版本回溯和协作同步。

**扩展性强**：Quill支持自定义模块和格式，可以根据需求扩展编辑器的功能。

本项目使用Quill 2.0.3版本，通过`@vueup/vue-quill`封装库集成到Vue 3项目中。

#### 2.1.6 Pinia状态管理

**Pinia** 是Vue 3官方推荐的状态管理库，完美适配Vue 3的组合式API风格。

**简化API**：相比Vuex，Pinia的API更加简洁直观，无需mutations、modules等复杂概念。

**TypeScript支持**：Pinia提供了完善的类型推断，可以自动推断store中的状态类型。

**模块化设计**：Pinia支持创建多个store，每个store管理独立的状态领域，便于代码组织。

**持久化支持**：Pinia可以轻松实现状态持久化，将状态保存到localStorage或Cookie。

本项目使用Pinia 3.0.4版本，管理用户登录状态、文档列表、协作状态等全局状态。

#### 2.1.7 其他前端技术

**Vue Router 4**：Vue 3的官方路由管理器，支持路由守卫、动态路由、嵌套路由等功能，用于实现单页应用的页面导航。

**Axios**：流行的HTTP客户端，用于与后端API进行通信，提供了请求拦截器、响应拦截器、自动JSON转换等功能。

**html2pdf.js**：客户端PDF生成库，可以将HTML内容转换为PDF文档，本项目实现了文档的导出功能。

**Marked**：快速的Markdown解析器，用于解析和渲染Markdown格式文本。

### 2.2 后端技术

#### 2.2.1 FastAPI

**FastAPI** 是一个现代、快速的Python Web框架，用于构建API服务。

**异步支持**：FastAPI基于Python 3.6+的async/await语法，利用Starlette作为ASGI框架和Uvicorn作为ASGI服务器，实现了高效的异步处理。这使得FastAPI在处理IO密集型任务（如数据库查询、HTTP请求）时具有出色性能。

**自动文档**：FastAPI基于OpenAPI标准自动生成API文档，访问`/docs`即可获得交互式的Swagger UI界面，访问`/redoc`可获得ReDoc格式的文档。这大大简化了API的测试和文档维护工作。

**类型提示**：FastAPI利用Python的类型提示（Type Hints）进行数据验证和自动序列化，配合Pydantic可以轻松实现请求参数和响应数据的模型定义。

**高性能**：根据TechEmpower基准测试，FastAPI是Python生态中性能最高的Web框架之一，性能接近Go和Node.js。

本项目使用FastAPI 0.104.1版本，构建了完整的RESTful API服务。

#### 2.2.2 Uvicorn

**Uvicorn** 是一个基于ASGI的Python ASGI服务器实现，旨在提供高性能和低内存占用。

**ASGI规范**：Uvicorn遵循ASGI（Asynchronous Server Gateway Interface）规范，是运行FastAPI应用的标准服务器。

**Worker支持**：Uvicorn支持多worker模式，可以充分利用多核CPU提升并发处理能力。

**WebSocket支持**：Uvicorn原生支持WebSocket协议，为实时协作功能提供了基础。

本项目使用Uvicorn 0.24.0版本作为ASGI服务器运行FastAPI应用。

#### 2.2.3 SQLAlchemy

**SQLAlchemy** 是Python生态中最成熟的ORM（对象关系映射）框架。

**ORM映射**：SQLAlchemy提供了从Python类到数据库表的映射，通过面向对象的方式操作数据库，无需编写原始SQL语句。

**数据库无关**：SQLAlchemy支持MySQL、PostgreSQL、SQLite、Oracle等多种数据库，通过切换连接字符串即可切换数据库。

**查询构建器**：SQLAlchemy提供了强大的查询构建器，支持链式调用，可以构建复杂的查询条件。

**迁移支持**：SQLAlchemy配合Alembic可以管理数据库版本，实现数据库结构的增量变更。

本项目使用SQLAlchemy 2.0.23版本定义数据模型和数据库操作。

#### 2.2.4 Pydantic

**Pydantic** 是Python的数据验证和设置管理库，在FastAPI中用于定义请求和响应模型。

**类型验证**：Pydantic利用类型提示进行数据验证，自动转换数据类型，验证失败时返回清晰的错误信息。

**数据序列化**：Pydantic支持将ORM模型快速转换为JSON格式，简化了API响应数据的构建。

**嵌套模型**：Pydantic支持嵌套的模型定义，可以处理复杂的JSON数据结构。

本项目使用Pydantic 2.5.0版本定义API的数据模型。

#### 2.2.5 JWT认证

**JWT（JSON Web Token）** 是一种开放标准（RFC 7519），用于在各方之间安全地传输JSON格式的信息。

**无状态认证**：JWT是自包含的，包含用户身份信息，服务器无需存储会话信息，适合分布式架构。

**签名机制**：JWT使用签名防止篡改，服务器验证签名即可确认token的合法性。

**过期机制**：JWT可以设置过期时间，支持token的自动失效和刷新。

本项目使用`python-jose`库实现JWT的生成和验证。

#### 2.2.6 Passlib密码哈希

**Passlib** 是Python的密码哈希库，提供了多种密码哈希算法的实现。

**密码处理**：用户密码进行处理后存储，确保用户数据安全。支持密码验证功能。

**版本兼容**：Passlib保持了良好的向后兼容性，确保了系统的稳定运行。

#### 2.2.7 WebSocket实时通信

**WebSocket** 是一种在单个TCP连接上提供全双工通信的协议，是实现实时协作功能的关键技术。

**持久连接**：与HTTP请求-响应模式不同，WebSocket建立持久连接，服务器可以主动向客户端推送消息。

**低开销**：WebSocket在建立连接后传输数据时，帧头部开销很小，适合频繁的数据交换场景。

FastAPI原生支持WebSocket，通过`WebSocket`类可以方便地处理WebSocket连接。

### 2.3 数据库

#### 2.3.1 MySQL

**MySQL** 是最流行的开源关系型数据库，在Web应用领域有着广泛的应用。

**性能优异**：MySQL在读写性能方面表现出色，配合适当的索引可以处理海量数据。

**生态系统完善**：MySQL拥有成熟的客户端工具、监控工具和备份恢复方案。

**事务支持**：MySQL支持ACID事务，可以保证数据的一致性和完整性。

本项目默认使用MySQL 5.7+作为数据库。

#### 2.3.2 SQLite（开发环境）

**SQLite** 是一个轻量级的嵌入式数据库，无需单独的数据库服务器进程。

**零配置**：SQLite无需安装和配置，数据库就是一个文件，非常适合开发和测试环境。

**便携性**：SQLite数据库文件可以轻松复制和移动，便于数据迁移。

本项目也支持SQLite，方便开发者在没有MySQL环境的情况下进行开发调试。

---

## 第三章 系统需求分析

### 3.1 系统概述

**GongChiBigWork** 是一个面向团队协作场景的在线文档编辑与管理系统。该系统旨在为用户提供便捷的文档创建、编辑、分享和协作功能，帮助团队提升信息共享和协同工作的效率。

系统采用前后端分离的架构设计，前端使用Vue 3构建单页应用，提供流畅的用户交互体验；后端使用FastAPI构建RESTful API服务，提供高效的数据处理能力。通过WebSocket技术实现多人实时协作编辑功能，支持多个用户同时编辑同一文档并实时同步变更。

系统的核心用户群体包括：企业内部团队成员、远程协作的分布式团队成员、教育机构的师生群体等。系统需要满足不同规模团队的使用需求，从个人用户到百人团队都能够正常使用。

### 3.2 系统需求分析

#### 3.2.1 功能需求

根据系统定位和目标用户分析，系统需要实现以下核心功能：

**（1）用户管理功能**

用户注册与登录是系统的基础功能。新用户可以通过填写用户名、邮箱、密码等基本信息完成注册。已注册用户可以通过用户名和密码登录系统。系统需要实现JWT Token认证机制，确保用户身份的安全验证。

用户需要能够查看和修改个人信息，包括头像上传、个人简介等信息。系统需要提供密码修改功能，支持用户定期更换密码保护账户安全。

**（2）文档管理功能**

文档管理是系统的核心功能。用户可以创建新文档，编辑文档标题和内容，删除不再需要的文档。系统需要支持文档列表的分页展示，每页显示固定数量的文档条目，支持用户浏览大量文档。

文档搜索功能允许用户通过关键词搜索文档标题，快速定位目标文档。文档需要支持富文本编辑，提供格式工具栏支持文本格式化（加粗、斜体、下划线等）、段落格式（标题、列表、引用等）、插入元素（图片、链接、代码块等）。

系统还需要实现AI文档概括功能，利用大语言模型自动分析文档内容，生成简洁的摘要，帮助用户快速了解文档要点。

文档导出功能允许用户将编辑完成的文档导出为PDF格式，便于离线阅读和分享。

**（3）实时协作功能**

实时协作是本系统的特色功能。系统需要支持多个用户同时编辑同一文档，每个用户的编辑操作需要实时同步给其他协作者。

系统需要显示当前文档的在线用户列表，让用户了解谁正在参与编辑。光标位置同步功能让用户可以看到其他用户的光标位置，增强协作体验。

协作编辑需要处理并发冲突，确保所有用户的修改都能正确保存，不会出现数据丢失或覆盖的问题。

**（4）权限管理功能**

文档权限管理确保文档的安全访问。系统设计了三种权限级别：owner（所有者）拥有文档的全部权限，包括删除文档、分配权限；edit（编辑权限）可以编辑文档内容和参与协作；view（查看权限）只能浏览文档内容，不能进行修改。

文档所有者可以将权限分配给其他用户，也可以随时收回权限。权限变更需要实时生效，确保访问控制的准确性。

**（5）好友社交功能**

好友系统为文档协作提供了社交基础。用户可以通过搜索用户名查找其他用户，并查看其基本信息。

发送好友请求需要得到对方同意才能建立好友关系。用户可以查看已添加的好友列表，管理好友关系。

好友请求管理包括发送请求、接受请求、拒绝请求等操作，系统需要实时通知用户收到的好友请求。

**（6）评论交流功能**

评论功能为文档提供了交流渠道。用户可以在文档下方发表评论，提出问题或反馈意见。

评论需要支持嵌套回复，形成讨论线程。用户可以编辑或删除自己发表的评论。系统需要显示评论的发布时间和评论者信息。

#### 3.2.2 非功能需求

除了功能需求外，系统还需要满足以下非功能需求：

**（1）性能需求**

页面加载时间：系统首页和主要功能页面需要在2秒内完成加载，提供流畅的用户体验。

API响应时间：常规API请求的响应时间应控制在200毫秒以内，复杂查询的响应时间不超过1秒。

并发支持：系统需要能够支持100个以上的并发用户同时在线操作。

实时同步延迟：协作编辑的同步延迟应控制在100毫秒以内，确保用户感受到实时同步。

**（2）安全性需求**

密码安全：用户密码应进行加密处理后存储，确保用户数据安全。建议使用bcrypt等强哈希算法。

身份验证：敏感操作需要验证用户身份，API接口需要验证JWT Token的有效性。

权限控制：用户只能访问被授权的文档，未授权的访问需要返回适当的错误信息。

数据传输：系统应支持HTTPS协议，确保数据传输的安全性。

**（3）可用性需求**

用户界面：界面设计需要简洁直观，符合用户的使用习惯，主要功能应在三次点击内可达。

错误处理：系统需要优雅地处理各种异常情况，给用户提供清晰的错误提示。

兼容性：系统需要支持主流浏览器（Chrome、Firefox、Edge、Safari）的最新版本。

**（4）可维护性需求**

代码规范：代码需要遵循统一的编码规范，保持良好的可读性。

模块化设计：系统功能模块需要解耦，便于后续的功能扩展和维护。

日志记录：系统需要记录关键操作的日志，便于问题排查和系统监控。

---

## 第四章 系统设计

### 4.1 系统架构设计

#### 4.1.1 整体架构

本系统采用**前后端分离**的架构设计，整体架构分为三个层次：前端展示层、后端服务层、数据存储层。

```
+---------------------------------------------------------------+
|                      前端展示层                                |
|  +-------------+ +-------------+ +-------------------------+  |
|  |   Vue 3     | |  Element    | |   Quill Editor          |  |
|  |  SPA 应用   | |   Plus      | |   协作编辑               |  |
|  +-------------+ +-------------+ +-------------------------+  |
+---------------------------------------------------------------+
                            |
                    HTTP / WebSocket
                            |
+---------------------------------------------------------------+
|                      后端服务层                                |
|  +-------------+ +-------------+ +-------------------------+  |
|  |  FastAPI    | |  业务逻辑   | |   WebSocket             |  |
|  |  REST API   | |   服务      | |   实时通信              |  |
|  +-------------+ +-------------+ +-------------------------+  |
+---------------------------------------------------------------+
                            |
                      SQLAlchemy ORM
                            |
+---------------------------------------------------------------+
|                      数据存储层                                |
|  +-------------+ +-------------+ +-------------------------+  |
|  |    MySQL    | |   SQLite    | |   本地文件存储           |  |
|  |  主数据库   | | (开发环境)  | |   (头像等)               |  |
|  +-------------+ +-------------+ +-------------------------+  |
+---------------------------------------------------------------+
```

#### 4.1.2 前端架构

前端采用Vue 3 + TypeScript + Vite的技术栈，整体架构分为以下几个模块：

**视图层（Views）**：包含系统的各个页面组件，如登录页、注册页、文档列表页、编辑器页、好友管理页、个人中心页等。每个视图组件对应一个路由，负责页面的渲染和用户交互。

**组件层（Components）**：包含可复用的UI组件，如侧边栏导航、好友选择器、评论组件、头像上传组件等。组件层为视图层提供支持，提高代码复用性。

**服务层（Services）**：封装与后端API的交互逻辑，包括HTTP请求封装、WebSocket协作服务等。服务层为视图层提供数据支持，屏蔽底层通信细节。

**状态管理层（Stores）**：使用Pinia管理全局状态，包括用户登录状态、当前文档信息、协作会话状态等。状态层为应用提供响应式的数据流。

**路由层（Router）**：使用Vue Router管理页面路由，包括路由守卫（验证用户登录状态）、动态路由（根据用户权限加载不同页面）等。

#### 4.1.3 后端架构

后端采用FastAPI框架，整体架构采用分层设计：

**API路由层（API/Routes）**：定义系统的RESTful接口，包括用户认证、文档管理、好友管理、评论管理、权限管理等模块的接口。每个模块的接口独立定义，便于维护和扩展。

**业务逻辑层（Services）**：处理各模块的业务逻辑，如用户注册流程、文档创建逻辑、权限验证等。业务逻辑层调用数据层完成数据持久化。

**数据层（Database）**：使用SQLAlchemy定义数据模型和数据库操作。包括数据库连接管理、模型定义、CRUD操作等。

**核心模块（Core）**：提供系统级的功能支持，包括应用配置（Configuration）和安全工具（Security）。配置模块管理数据库连接、JWT密钥等配置信息；安全工具模块提供密码哈希、Token验证等功能。

#### 4.1.4 系统架构图

```
                          +------------------+
                          |    用户浏览器    |
                          +--------+---------+
                                   |
                          +--------▼---------+
                          |   Vue 3 前端     |
                          |  (localhost:5173)|
                          +--------+---------+
                                   |
              +--------------------┼--------------------+
              |                    |                    |
    +---------▼---------+  +------▼------+  +----------▼----------+
    |   HTTP API        |  |  WebSocket  |  |   静态资源          |
    |  /api/auth        |  |  /ws/collab |  |   /uploads          |
    |  /api/documents   |  |             |  |                     |
    |  /api/friends     |  |             |  +--------------------+
    |  /api/comments    |  |             |
    +---------+---------+  +------+-------+
              |                    |
              +--------+------------+
                           |
              +------------▼------------+
              |     FastAPI 后端         |
              |   (localhost:8000)       |
              +------------+------------+
                           |
              +------------▼------------+
              |    SQLAlchemy ORM       |
              +------------+------------+
                           |
         +-----------------++-----------------+
         |                 |                 |
+--------▼--------+  +-----▼-----+  +--------▼--------+
|     MySQL       |  |  SQLite   |  |   文件系统       |
|  核心业务数据    |  |  开发环境  |  |   头像存储       |
+-----------------+  +-----------+  +------------------+
```

### 4.2 功能模块划分

根据需求分析，系统划分为以下主要功能模块：

#### 4.2.1 用户认证模块

用户认证模块负责用户的注册、登录和身份验证功能。

**主要功能**：

- 用户注册：接收用户名、邮箱、密码等注册信息，验证信息有效性，创建用户记录
- 用户登录：验证用户名和密码，生成并返回JWT Token
- 获取当前用户：根据Token获取当前登录用户的信息
- Token刷新：在Token即将过期时刷新，延长会话时间

**模块边界**：该模块只处理与用户身份相关的操作，不涉及文档、好友等其他业务。

#### 4.2.2 文档管理模块

文档管理模块负责文档的完整生命周期管理。

**主要功能**：

- 创建文档：创建新的空白文档，设置初始标题
- 文档列表：分页获取当前用户的文档列表
- 文档详情：获取指定文档的详细内容
- 更新文档：修改文档标题和富文本内容
- 删除文档：删除指定的文档
- 文档搜索：根据关键词搜索文档标题
- AI概括：调用AI服务生成文档摘要
- PDF导出：将文档内容转换为PDF格式

**模块边界**：文档模块只负责文档本身的CRUD操作，文档权限由权限模块负责，文档评论由评论模块负责。

#### 4.2.3 实时协作模块

实时协作模块负责多人同时编辑同一文档的功能。

**主要功能**：

- 建立协作：WebSocket连接建立，进入协作会话
- 内容同步：实时同步各用户的编辑内容
- 光标同步：同步显示各用户的光标位置
- 在线用户：维护当前在线用户列表
- 断开协作：WebSocket连接断开，退出协作会话

**模块边界**：协作模块专注于实时通信，文档内容的最终持久化由文档模块负责。

#### 4.2.4 权限管理模块

权限管理模块负责文档的访问控制。

**主要功能**：

- 查看权限：获取文档的权限列表
- 授予权限：为用户授予文档的编辑或查看权限
- 收回权限：撤销用户对文档的权限
- 权限验证：验证用户是否有权访问文档

**模块边界**：权限模块只管理文档的访问权限，用户的好友关系由好友模块管理。

#### 4.2.5 好友社交模块

好友社交模块负责用户之间的社交关系管理。

**主要功能**：

- 用户搜索：根据用户名搜索其他用户
- 好友列表：获取当前用户的好友列表
- 发送请求：向其他用户发送好友请求
- 处理请求：接受或拒绝好友请求
- 删除好友：移除好友关系

**模块边界**：好友模块只管理社交关系，不涉及文档分享，分享功能由权限模块负责。

#### 4.2.6 评论交流模块

评论交流模块负责文档评论功能。

**主要功能**：

- 添加评论：在文档下方添加评论
- 评论列表：获取文档的所有评论
- 编辑评论：修改已发表的评论
- 删除评论：删除已发表的评论
- 嵌套回复：在评论下回复，形成讨论串

**模块边界**：评论模块只管理评论内容，评论的权限与文档权限关联。

### 4.3 数据库表设计

#### 4.3.1 数据库ER图

```
+--------------+       +-----------------+       +--------------+
|    users     |       |   documents    |       |   friends    |
+--------------+       +-----------------+       +--------------+
| id (PK)      |<------| owner_id (FK)   |       | user_id (FK) |
| username     |       | id (PK)        |       | friend_id(FK)|
| email        |       | title          |       | (PK)         |
| password_hash|       | content        |       +--------------+
| avatar       |       | created_at     |
| created_at   |       | updated_at     |
+--------------+       +-----------------+
        |                       |
        |                       | 1:n
        |              +--------+---------+
        |              | document_         |
        |              | permissions       |
        |              +-------------------+
        |              | id (PK)           |
        |              | document_id (FK)  |
        |              | user_id (FK)      |
        |              | permission_level |
        +--------------+-------------------+

+------------------+       +---------------------+
|  friend_requests |       |     comments        |
+------------------+       +---------------------+
| id (PK)          |       | id (PK)             |
| from_user_id(FK) |       | document_id (FK)    |
| to_user_id (FK)  |       | user_id (FK)        |
| status           |       | parent_id (FK,NULL) |
| created_at       |       | content             |
+------------------+       | created_at          |
                           | updated_at          |
                           +---------------------+
```

#### 4.3.2 数据表详细设计

**（1）用户表（users）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | String(50) | UNIQUE, NOT NULL | 用户名 |
| email | String(100) | UNIQUE, NOT NULL | 邮箱 |
| password_hash | String(255) | NOT NULL | 密码哈希 |
| avatar | String(500) | NULL | 头像URL |
| created_at | DateTime | NOT NULL | 创建时间 |

```python
class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True, index=True)
    username = Column(String(50), unique=True, index=True, nullable=False)
    email = Column(String(100), unique=True, index=True, nullable=False)
    password_hash = Column(String(255), nullable=False)
    avatar = Column(String(500), nullable=True)
    created_at = Column(DateTime, default=datetime.utcnow)
```

**（2）文档表（documents）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 文档ID |
| title | String(200) | NOT NULL | 文档标题 |
| content | Text | NULL | 富文本HTML内容 |
| owner_id | Integer | FOREIGN KEY -> users.id | 所有者ID |
| created_at | DateTime | NOT NULL | 创建时间 |
| updated_at | DateTime | NOT NULL | 更新时间 |

```python
class Document(Base):
    __tablename__ = "documents"
    id = Column(Integer, primary_key=True, index=True)
    title = Column(String(200), default="Untitled Document")
    content = Column(Text, nullable=True)
    owner_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

**（3）文档权限表（document_permissions）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 权限ID |
| document_id | Integer | FOREIGN KEY -> documents.id | 文档ID |
| user_id | Integer | FOREIGN KEY -> users.id | 用户ID |
| permission_level | String(20) | NOT NULL | 权限级别 |

```python
class DocumentPermission(Base):
    __tablename__ = "document_permissions"
    id = Column(Integer, primary_key=True, index=True)
    document_id = Column(Integer, ForeignKey("documents.id"), nullable=False)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    permission_level = Column(String(20), nullable=False)
```

**（4）好友表（friends）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| user_id | Integer | FOREIGN KEY -> users.id, PRIMARY KEY | 用户ID |
| friend_id | Integer | FOREIGN KEY -> users.id, PRIMARY KEY | 好友ID |

```python
class Friend(Base):
    __tablename__ = "friends"
    user_id = Column(Integer, ForeignKey("users.id"), primary_key=True)
    friend_id = Column(Integer, ForeignKey("users.id"), primary_key=True)
```

**（5）好友请求表（friend_requests）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 请求ID |
| from_user_id | Integer | FOREIGN KEY -> users.id | 发送者ID |
| to_user_id | Integer | FOREIGN KEY -> users.id | 接收者ID |
| status | String(20) | NOT NULL | 请求状态 |
| created_at | DateTime | NOT NULL | 创建时间 |

```python
class FriendRequest(Base):
    __tablename__ = "friend_requests"
    id = Column(Integer, primary_key=True, index=True)
    from_user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    to_user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    status = Column(String(20), default="pending")
    created_at = Column(DateTime, default=datetime.utcnow)
```

**（6）评论表（comments）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 评论ID |
| document_id | Integer | FOREIGN KEY -> documents.id | 文档ID |
| user_id | Integer | FOREIGN KEY -> users.id | 评论者ID |
| parent_id | Integer | FOREIGN KEY -> comments.id, NULL | 父评论ID |
| content | Text | NOT NULL | 评论内容 |
| created_at | DateTime | NOT NULL | 创建时间 |
| updated_at | DateTime | NOT NULL | 更新时间 |

```python
class Comment(Base):
    __tablename__ = "comments"
    id = Column(Integer, primary_key=True, index=True)
    document_id = Column(Integer, ForeignKey("documents.id"), nullable=False)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    parent_id = Column(Integer, ForeignKey("comments.id"), nullable=True)
    content = Column(Text, nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
```

---

## 第五章 系统实现

### 5.1 通用部分实现方案

#### 5.1.1 项目初始化配置

**前端项目初始化**

前端项目使用Vite进行初始化和构建。通过`npm create vite@latest`命令创建项目，选择Vue + TypeScript模板。项目的主要配置文件包括：

`vite.config.ts`：配置Vue插件、路径别名、代理等

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import vueJsx from '@vitejs/plugin-vue-jsx'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue(), vueJsx()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
})
```

`tsconfig.json`：配置TypeScript编译选项，设置严格类型检查

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  }
}
```

**后端项目初始化**

后端项目使用FastAPI框架。项目结构遵循分层设计原则：

```
backend/
├── app/
│   ├── api/          # API路由
│   ├── core/        # 核心配置
│   └── db/          # 数据库层
├── scripts/        # SQL脚本
├── requirements.txt # 依赖列表
└── run.py          # 启动文件
```

`requirements.txt`：定义Python依赖及其版本

```
fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
pymysql==1.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4  # 密码处理库（当前未启用bcrypt）
pydantic==2.5.0
python-multipart==0.0.6
```

#### 5.1.2 数据库连接与配置

**数据库配置模块** (`backend/app/core/config.py`)

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str = "sqlite:///./gongchibigwork.db"
    SECRET_KEY: str = "your-secret-key-here"
    ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 30
    
    class Config:
        env_file = ".env"

settings = Settings()
```

**数据库连接** (`backend/app/db/database.py`)

```python
from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker
from app.core.config import settings

engine = create_engine(settings.DATABASE_URL, connect_args={"check_same_thread": False})
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

#### 5.1.3 认证与安全

**密码处理** (`backend/app/core/security.py`)

当前实现使用Passlib配置，但实际存储明文密码：

```python
from passlib.context import CryptContext

# 配置bcrypt（当前未实际使用）
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def verify_password(plain_password: str, hashed_password: str) -> bool:
    # 实际为直接比较明文
    return plain_password == hashed_password

def get_password_hash(password: str) -> str:
    # 实际为直接返回原密码（明文存储）
    return password
```

> ⚠️ **安全提示**：当前实现为明文存储密码，建议生产环境使用bcrypt加密：
> ```python
> def verify_password(plain_password: str, hashed_password: str) -> bool:
>     return pwd_context.verify(plain_password, hashed_password)
> 
> def get_password_hash(password: str) -> str:
>     return pwd_context.hash(password)
> ```

**JWT Token生成与验证**：

```python
from datetime import datetime, timedelta
from jose import JWTError, jwt

def create_access_token(data: dict):
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    encoded_jwt = jwt.encode(to_encode, settings.SECRET_KEY, algorithm=settings.ALGORITHM)
    return encoded_jwt

def verify_token(token: str):
    try:
        payload = jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])
        return payload
    except JWTError:
        return None
```

#### 5.1.4 API服务封装

前端通过Axios封装HTTP请求，提供统一的请求和响应处理：

```typescript
import axios from 'axios'

const apiClient = axios.create({
  baseURL: '/api',
  headers: {
    'Content-Type': 'application/json'
  }
})

apiClient.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

apiClient.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)

export default apiClient
```

### 5.2 核心功能实现

#### 5.2.1 用户认证功能

**后端实现**

用户认证API路由定义在`backend/app/api/auth.py`：

- **用户注册** (`POST /register`)

```python
@router.post("/register", response_model=UserResponse)
def register(user: UserCreate, db: Session = Depends(get_db)):
    db_user = db.query(User).filter(User.username == user.username).first()
    if db_user:
        raise HTTPException(status_code=400, detail="Username already registered")
    
    hashed_password = get_password_hash(user.password)
    new_user = User(
        username=user.username,
        email=user.email,
        password_hash=hashed_password
    )
    db.add(new_user)
    db.commit()
    db.refresh(new_user)
    return new_user
```

- **用户登录** (`POST /login`)

```python
@router.post("/login")
def login(user: UserLogin, db: Session = Depends(get_db)):
    db_user = db.query(User).filter(User.username == user.username).first()
    if not db_user or not verify_password(user.password, db_user.password_hash):
        raise HTTPException(status_code=401, detail="Incorrect username or password")
    
    access_token = create_access_token(data={"sub": db_user.username})
    return {"access_token": access_token, "token_type": "bearer"}
```

**前端实现**

登录页面 (`src/views/AboutView.vue`)：

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import apiClient from '@/services/api'

const router = useRouter()
const loginForm = ref({
  username: '',
  password: ''
})

const handleLogin = async () => {
  try {
    const response = await apiClient.post('/auth/login', loginForm.value)
    localStorage.setItem('token', response.data.access_token)
    ElMessage.success('登录成功')
    router.push('/table')
  } catch (error: any) {
    ElMessage.error(error.response?.data?.detail || '登录失败')
  }
}
</script>
```

#### 5.2.2 文档管理功能

**后端实现**

文档API路由定义在`backend/app/api/documents.py`：

- **创建文档** (`POST /`)

```python
@router.post("/", response_model=DocumentResponse)
def create_document(
    document: DocumentCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    new_document = Document(
        title=document.title,
        content=document.content,
        owner_id=current_user.id
    )
    db.add(new_document)
    db.commit()
    db.refresh(new_document)
    return new_document
```

- **获取文档列表** (`GET /`)

```python
@router.get("/", response_model=List[DocumentResponse])
def get_documents(
    skip: int = 0,
    limit: int = 10,
    search: str = None,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    query = db.query(Document).filter(Document.owner_id == current_user.id)
    
    if search:
        query = query.filter(Document.title.contains(search))
    
    documents = query.offset(skip).limit(limit).all()
    return documents
```

- **AI文档概括** (`POST /ai-summary`)

```python
@router.post("/ai-summary")
async def summarize_document(
    request: AISummaryRequest,
    db: Session = Depends(get_db)
):
    document = db.query(Document).filter(Document.id == request.document_id).first()
    if not document:
        raise HTTPException(status_code=404, detail="Document not found")
    
    summary = await call_ai_service(document.content)
    return {"summary": summary}
```

**前端实现**

文档列表页面 (`src/views/TableView.vue`)：

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import apiClient from '@/services/api'

const router = useRouter()
const documents = ref([])
const loading = ref(false)

const fetchDocuments = async () => {
  loading.value = true
  try {
    const response = await apiClient.get('/documents/')
    documents.value = response.data
  } catch (error) {
    ElMessage.error('获取文档列表失败')
  } finally {
    loading.value = false
  }
}

const createDocument = async () => {
  try {
    const response = await apiClient.post('/documents/', {
      title: 'Untitled Document',
      content: ''
    })
    router.push(`/editor?id=${response.data.id}`)
  } catch (error) {
    ElMessage.error('创建文档失败')
  }
}

onMounted(fetchDocuments)
</script>
```

#### 5.2.3 实时协作功能

实时协作是本系统的核心特色功能，基于WebSocket实现多人同时编辑同一文档的实时同步。以下详细介绍其实现方案。

**5.2.3.1 WebSocket架构设计**

系统采用"房间"模式管理协作会话，每个文档对应一个独立的"房间"：

- **连接管理器（ConnectionManager）**：核心类，负责管理所有WebSocket连接
  - `active_connections: Dict[int, Set[WebSocket]]` - 按文档ID分组存储连接
  - `online_users: Dict[int, Set[str]]` - 记录每个文档房间的在线用户
  - `document_contents: Dict[int, str]` - 缓存文档内容用于同步

- **消息类型**：系统定义了多种消息类型处理不同协作场景
  - `join` - 用户加入协作房间
  - `content_change` - 内容变更（Quill Delta格式）
  - `cursor_position` - 光标位置同步
  - `prepare_sync` - 同步前准备
  - `sync_content` - 内容同步
  - `sync_users` - 用户列表同步
  - `user_joined` - 用户加入通知
  - `user_left` - 用户离开通知
  - `document_saved` - 文档保存通知
  - `ping/pong` - 心跳保活

**5.2.3.2 用户加入与同步机制**

当新用户加入协作房间时，系统执行以下流程：

1. **加入前同步**：先通知房间内现有用户保存当前内容，避免新成员收到过时的编辑操作
2. **等待1秒**：给老用户完成数据库保存
3. **正式入房**：将新用户加入房间，更新在线列表
4. **推送最新内容**：从数据库获取文档最新内容推送给新用户
5. **广播加入**：通知其他用户更新在线列表

```python
async def connect(self, websocket: WebSocket, document_id: int, username: str):
    # 1. 通知老用户保存
    await self.broadcast_to_document(document_id, {
        "type": "prepare_sync",
        "username": username,
        "trigger_save": True,
    })
    
    # 2. 等待保存完成
    await asyncio.sleep(1.0)
    
    # 3. 新成员入房
    self.active_connections[document_id].add(websocket)
    self.online_users[document_id].add(username)
    
    # 4. 推送数据库中的最新内容
    document = db.query(Document).filter(Document.id == document_id).first()
    await websocket.send_json({
        "type": "sync_content",
        "content": document.content if document.content else "",
    })
    
    # 5. 广播用户加入
    await self.broadcast_to_document(document_id, {
        "type": "user_joined",
        "username": username,
        "users": list(self.online_users[document_id]),
    })
```

**5.2.3.3 内容变更广播**

当用户编辑文档时，系统通过以下流程同步给其他用户：

1. 接收客户端发送的Delta格式变更（Quill编辑器的操作指令）
2. 排除发送者，将Delta广播给房间内其他用户
3. 其他用户收到后应用Delta到本地编辑器

```python
async def broadcast_content_change(
    self, document_id: int, username: str, delta: dict, 
    source: str, exclude: WebSocket | None = None
):
    await self.broadcast_to_document(
        document_id,
        {
            "type": "content_change",
            "username": username,
            "delta": delta,
            "source": "remote",  # 标识为远程变更
        },
        exclude,  # 排除发送者
    )
```

**5.2.3.4 光标位置同步**

系统实时同步用户的光标位置，其他用户可以看到协作者的光标：

```python
async def broadcast_cursor_position(self, document_id: int, username: str, cursor: dict):
    await self.broadcast_to_document(
        document_id,
        {
            "type": "cursor_position",
            "username": username,
            "cursor": cursor,
        },
        None,  # 发送给所有用户包括发送者
    )
```

**5.2.3.5 用户离开处理**

用户离开时触发以下处理：

1. 从房间移除该用户的连接
2. 广播用户离开消息给其他用户
3. 请求其他用户保存当前文档内容
4. 如果房间为空，清理房间资源

```python
def disconnect(self, websocket: WebSocket, document_id: int, username: str):
    self.active_connections[document_id].discard(websocket)
    self.online_users[document_id].discard(username)
    
    # 房间为空时清理资源
    if not self.active_connections[document_id]:
        del self.active_connections[document_id]
        del self.online_users[document_id]
```

**5.2.3.6 WebSocket端点**

后端提供以下WebSocket端点：

```python
@router.websocket("/ws/collaborate/{document_id}")
async def websocket_collaborate(websocket: WebSocket, document_id: int):
    """协作编辑WebSocket端点"""
    # 1. 接受连接
    await websocket.accept()
    
    # 2. 接收用户名
    data = await websocket.receive_json()
    username = data.get("username", "匿名用户")
    
    # 3. 建立连接
    await manager.connect(websocket, document_id, username)
    
    # 4. 消息处理循环
    while True:
        data = await websocket.receive_json()
        message_type = data.get("type")
        
        if message_type == "content_change":
            # 处理内容变更
            await manager.broadcast_content_change(...)
        elif message_type == "cursor_position":
            # 处理光标位置
            await manager.broadcast_cursor_position(...)
        elif message_type == "ping":
            # 心跳
            await websocket.send_json({"type": "pong"})
```

**5.2.3.7 前端协作服务**

前端通过`CollaborationService`类封装WebSocket操作：

```typescript
export class CollaborationService {
  private ws: WebSocket | null = null
  
  // 事件回调
  public onContentUpdate: ((data: any) => void) | null = null
  public onUserJoined: ((userId: number) => void) | null = null
  public onUserLeft: ((userId: number) => void) | null = null
  
  constructor(documentId: number) {
    this.documentId = documentId
    this.connect()
  }
  
  private connect() {
    const token = localStorage.getItem('token')
    this.ws = new WebSocket(
      `ws://localhost:8000/ws/collaborate/${this.documentId}`
    )
    
    this.ws.onmessage = (event) => {
      const data = JSON.parse(event.data)
      switch (data.type) {
        case 'content_change':
          this.onContentUpdate?.(data)
          break
        case 'user_joined':
          this.onUserJoined?.(data.user_id)
          break
        case 'user_left':
          this.onUserLeft?.(data.user_id)
          break
      }
    }
  }
  
  // 发送内容变更
  sendContentUpdate(content: string, cursor: any) {
    this.ws?.send(JSON.stringify({
      type: 'content_change',
      content,
      cursor
    }))
  }
  
  // 断开连接
  disconnect() {
    this.ws?.close()
  }
}
```

**5.2.3.8 技术特点总结**

1. **Delta同步**：使用Quill的Delta格式传输编辑操作，而非完整内容，节省带宽
2. **冲突避免**：通过排除发送者避免循环更新
3. **离线保存**：用户加入/离开时触发保存，确保数据不丢失
4. **心跳机制**：支持ping/pong心跳保持连接活跃
5. **在线用户**：实时维护和展示在线用户列表

  constructor(documentId: number) {
    this.documentId = documentId
    this.connect()
  }

  private connect() {
    const token = localStorage.getItem('token')
    this.ws = new WebSocket(`ws://localhost:8000/ws/collaborate/${this.documentId}?token=${token}`)

    this.ws.onmessage = (event) => {
      const data = JSON.parse(event.data)
      switch (data.type) {
        case 'content_update':
          this.onContentUpdate?.(data)
          break
        case 'user_joined':
          this.onUserJoined?.(data.user_id)
          break
        case 'user_left':
          this.onUserLeft?.(data.user_id)
          break
      }
    }
  }

  sendContentUpdate(content: string, cursor: any) {
    this.ws?.send(JSON.stringify({
      type: 'content_update',
      content,
      cursor
    }))
  }

  disconnect() {
    this.ws?.close()
  }
}
```

协作编辑器页面 (`src/views/CollabEditorView.vue`)：

```vue
<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'
import { useRoute } from 'vue-router'
import { QuillEditor } from '@vueup/vue-quill'
import '@vueup/vue-quill/dist/vue-quill.snow.css'
import { CollaborationService } from '@/services/collaboration'

const route = useRoute()
const documentId = Number(route.query.id)
const content = ref('')
const onlineUsers = ref<number[]>([])
let collabService: CollaborationService | null = null

onMounted(async () => {
  collabService = new CollaborationService(documentId)
  
  collabService.onContentUpdate = (data) => {
    content.value = data.content
  }
  
  collabService.onUserJoined = (userId) => {
    onlineUsers.value.push(userId)
  }
  
  collabService.onUserLeft = (userId) => {
    onlineUsers.value = onlineUsers.value.filter(id => id !== userId)
  }
})

const handleContentChange = (newContent: string) => {
  collabService?.sendContentUpdate(newContent, null)
}

onUnmounted(() => {
  collabService?.disconnect()
})
</script>
```

#### 5.2.4 权限控制功能

**后端实现**

权限管理API (`backend/app/api/document_permissions.py`)：

```python
@router.post("/{document_id}/permissions")
def grant_permission(
    document_id: int,
    permission: PermissionGrant,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    document = db.query(Document).filter(Document.id == document_id).first()
    if document.owner_id != current_user.id:
        raise HTTPException(status_code=403, detail="Not authorized")
    
    new_permission = DocumentPermission(
        document_id=document_id,
        user_id=permission.user_id,
        permission_level=permission.permission_level
    )
    db.add(new_permission)
    db.commit()
    return {"message": "Permission granted"}
```

**权限验证中间件**：

```python
def check_document_permission(
    document_id: int,
    required_level: str,
    db: Session,
    current_user: User
):
    document = db.query(Document).filter(Document.id == document_id).first()
    if not document:
        raise HTTPException(status_code=404, detail="Document not found")
    
    if document.owner_id == current_user.id:
        return True
    
    permission = db.query(DocumentPermission).filter(
        DocumentPermission.document_id == document_id,
        DocumentPermission.user_id == current_user.id
    ).first()
    
    if not permission:
        raise HTTPException(status_code=403, detail="No permission")
    
    if required_level == "edit" and permission.permission_level != "edit":
        raise HTTPException(status_code=403, detail="Insufficient permission")
    
    return True
```

#### 5.2.5 好友系统功能

**后端实现**

好友API (`backend/app/api/friends.py`)：

```python
@router.get("/search")
def search_users(
    query: str,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    users = db.query(User).filter(
        User.username.contains(query)
    ).limit(10).all()
    return users

@router.post("/request")
def send_friend_request(
    request: FriendRequestCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    new_request = FriendRequest(
        from_user_id=current_user.id,
        to_user_id=request.to_user_id
    )
    db.add(new_request)
    db.commit()
    return {"message": "Friend request sent"}

@router.post("/request/{request_id}/accept")
def accept_request(
    request_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    friend_request = db.query(FriendRequest).filter(
        FriendRequest.id == request_id,
        FriendRequest.to_user_id == current_user.id
    ).first()
    
    if not friend_request:
        raise HTTPException(status_code=404, detail="Request not found")
    
    # 创建好友关系（双向）
    friend1 = Friend(user_id=current_user.id, friend_id=friend_request.from_user_id)
    friend2 = Friend(user_id=friend_request.from_user_id, friend_id=current_user.id)
    db.add(friend1)
    db.add(friend2)
    
    friend_request.status = "accepted"
    db.commit()
    return {"message": "Friend added"}
```

#### 5.2.6 评论系统功能

**后端实现**

评论API (`backend/app/api/comments.py`)：

```python
@router.post("/{document_id}/comments")
def add_comment(
    document_id: int,
    comment: CommentCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    new_comment = Comment(
        document_id=document_id,
        user_id=current_user.id,
        parent_id=comment.parent_id,
        content=comment.content
    )
    db.add(new_comment)
    db.commit()
    db.refresh(new_comment)
    return new_comment

@router.get("/{document_id}/comments")
def get_comments(
    document_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    comments = db.query(Comment).filter(
        Comment.document_id == document_id
    ).all()
    return comments
```

---

## 第六章 总结与展望

### 6.1 总结

本文详细阐述了基于Vue 3与FastAPI的协作编辑平台的设计与实现过程。项目采用前后端分离的现代化Web架构，前端使用Vue 3 + TypeScript + Vite构建单页应用，后端使用FastAPI + SQLAlchemy构建RESTful API服务，通过WebSocket技术实现了多人实时协作编辑功能。

项目主要完成了以下工作：

**（1）系统架构设计**

完成了系统的整体架构设计，采用分层架构将系统划分为前端展示层、后端服务层和数据存储层。前端采用Vue 3 + Element Plus构建了美观易用的用户界面，后端采用FastAPI构建了高效可靠的API服务。

**（2）核心功能实现**

完成了用户认证、文档管理、实时协作、权限控制、好友社交、评论系统等核心功能的开发。用户可以通过系统创建、编辑、分享文档，与团队成员进行实时协作。

**（3）技术栈应用**

深入应用了Vue 3的组合式API、TypeScript的类型系统、Pinia状态管理、Element Plus组件库等前端技术；以及FastAPI的异步编程、SQLAlchemy ORM、JWT认证、WebSocket通信等后端技术。

**（4）数据库设计**

设计了完整的数据库表结构，包括用户表、文档表、权限表、好友表、好友请求表、评论表等，完成了数据模型和关系的建模。

### 6.2 展望

尽管系统已经实现了协作编辑平台的核心功能，但仍有以下几个方面可以改进和扩展：

**（1）冲突解决机制优化**

当前系统采用的简单广播机制在高并发场景下可能产生冲突。未来可以引入 Operational Transformation (OT) 或 CRDT (Conflict-free Replicated Data Types) 算法，提供更完善的冲突解决机制。

**（2）离线编辑支持**

目前系统需要在线编辑，未来可以引入本地存储和离线同步机制，支持用户在网络不稳定的环境下编辑文档，恢复网络后自动同步。

**（3）版本管理功能**

可以增加文档版本管理功能，记录文档的历史修改版本，支持版本回溯和对比。

**（4）移动端适配**

目前系统主要面向桌面端用户，未来可以优化移动端体验，实现响应式设计，支持移动设备上的文档编辑。

**（5）AI功能扩展**

目前系统已实现AI文档概括功能，未来可以扩展更多AI功能，如智能排版、内容润色、关键词提取、自动翻译等。

**（6）性能优化**

可以引入缓存机制、数据库索引优化、CDN加速等技术，提升系统的整体性能，支持更大规模的用户并发访问。

综上所述，本项目为协作编辑平台的建设提供了一个可行的技术方案，随着后续的迭代优化，系统将具备更强的功能和更好的用户体验。

---

*文档完*
